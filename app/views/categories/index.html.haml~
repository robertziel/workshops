%h1 Listing categories


.row
  - categories.each do |category|
    .col-sm-4
      .p
        %p= category.name
        %p= link_to 'Show', category, :class => "btn btn-primary large"
        - if current_user.try(:admin?)
          %p= link_to 'Edit', edit_category_path(category), :class => "btn btn-primary large"
          %p= link_to 'Destroy', category, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger large"

%br

= link_to 'New Category', new_category_path, :class => "btn btn-green large"
