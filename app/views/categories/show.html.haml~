%p#notice= notice

%p
  %b Name:
  = category.name


.row
  - category.products.each do |product|
    .col-lg-3.col-md-4.col-sm-6.col-xs-12
      .product.cont
        %p= link_to product.title, category_product_path(product.category, product), :class => "title"
        %p= product.description
        %p= "#{product.price} PLN"
        %p.bt= link_to 'Show', category_product_path(product.category, product), :class => "btn btn-primary large"
        - if current_user.try(:admin?)
          %p.bt= link_to 'Edit', edit_category_product_path(product.category, product), :class => "btn btn-primary large"
          %p.bt= link_to 'Destroy', category_product_path(product.category, product), method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger large"

%br

%p
  %h3 New product
  = render 'products/form'

= link_to 'Edit', edit_category_path(category), class: 'btn btn-primary'

= link_to 'Back', categories_path, class: 'btn btn-primary'
