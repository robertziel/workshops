%p#notice= notice

%p
  %b Name:
  = category.name


.row
  - category.products.each do |product|
    .col-lg-2.col-md-2.col-sm-4.col-xs-6
      .product.cont
        %p= product.title
        %p= product.description
        %p= product.price
        %p= link_to 'Show', category_product_path(product.category, product), :class => "btn btn-primary large"
        - if current_user.try(:admin?)
          %p= link_to 'Edit', edit_category_product_path(product.category, product), :class => "btn btn-primary large"
          %p= link_to 'Destroy', category_product_path(product.category, product), method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger large"

%br

%p
  %h3 New product
  = render 'products/form'

= link_to 'Edit', edit_category_path(category)
\|
= link_to 'Back', categories_path, class: 'btn btn-primary'
